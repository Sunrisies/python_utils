## 附录B：运算符与符号

此附录包含Rust语法的词汇表，其中包括运算符等内容。
其他单独出现或在路径、泛型上下文中出现的符号。
特质边界、宏、属性、注释、元组以及方括号。

### 运算符

表B-1列出了Rust语言中的运算符，以及这些运算符的用法示例。
在上下文中出现，附有简短的解释，以及该运算符是否为
可重载。如果一个运算符是可重载的，那么需要使用的相关特性就是
该运算符的重载选项已列出。

<span class="caption"> 表B-1：运算符</span>


| 运算符                    | 示例                                                 | 解释                                                           | 可重载吗? |
| ------------------------- | ------------------------------------------------------- | --------------------------------------------------------------------- | -------- |
| `!`                   | `ident!(...)`, `ident!{...}`, `ident![...]`             | 宏展开                                                       |          |
| `!`                   | `!expr`                                                 | 位或逻辑补数                                                     | `Not`  |
| `!=`                  | `expr != expr`                                          | 不等比较                                                       | `PartialEq`  |
| `%`                  | `expr % expr`                                           | 算术取余器                                                     | `Rem`  |
| `%=`                  | `var %= expr`                                           | 算术取余器及赋值操作                                         | `RemAssign`  |
| `&`                  | `&expr`, `&mut expr`                                    | 借用操作                                                         |          |
| `&`                  | `&type`, `&mut type`, `&'a type`, `&'a mut type`        | 借用指针类型                                                  |          |
| `&`                  | `expr & expr`                                           | 位与操作                                                         | `BitAnd`  |
| `&=`                  | `var &= expr`                                           | 位与操作及赋值操作                                         | `BitAndAssign`  |
| `&&`                  | `expr && expr`                                          | 逻辑与短路操作                                                |          |
| `*`                  | `expr * expr`                                           | 算术乘法操作                                                 | `Mul`  |
| `*=`                  | `var *= expr`                                           | 算术乘法操作及赋值操作                                     | `MulAssign`  |
| `*`                  | `*expr`                                                 | 解引用操作                                                     | `Deref`  |
| `*`                   | `*const type`, `*mut type`                               | 原始指针                                                           |          |
| `+`                   | `trait + trait`, `'a + trait`                               | 复合类型约束                                                      |          |
| `+`                   | `expr + expr`                                           | 算术加法                                                           | `Add`  |
| `+=`                  | `var += expr`                                           | 算术加法与赋值                                                     | `AddAssign`  |
| `,`                  | `expr, expr`                                          | 参数与元素分隔符                                                  |          |
| `-`                  | `- expr`                                           | 算术取反                                                           | `Neg`    |
| `-`                  | `expr - expr`                                           | 算术减法                                                           | `Sub`    |
| `-=`                  | `var -= expr`                                           | 算术减法与赋值                                                     | `SubAssign`  |
| `->`                  | `fn(...) -> type`, <code>&vert;...&vert; -> type</code> | 函数与闭包返回类型                                                |          |
| `.`                  | `expr.ident`                                           | 字段访问                                                          |          |
| `.`                  | `expr.ident(expr, ...)`                                         | 方法调用                                                           |          |
| `.`                  | `expr.0`, `expr.1`, 等等                           | 元组索引                                                        |          |
| `..`                   | `..`, `expr..`, `..expr`, `expr..expr`                  | 右排斥范围字面量                                              | `PartialOrd` |
| `..=`                   | `..=expr`, `expr..=expr`                                | 右包含范围字面量                                              | `PartialOrd`  |
| `..`                  | `..expr`                                                | 结构体字面量更新语法                                          |           |
| `..`                  | `variant(x, ..)`, `struct_type { x, .. }`               | “以及其余”模式绑定                                        |           |
| `...`                  | `expr...expr`                                           | （已弃用，改用 `..=`）在模式中：包含范围模式           |           |
| `/`                  | `expr / expr`                                           | 算术除法                                                     | `Div`   |
| `/=`                  | `var /= expr`                                           | 算术除法与赋值                                                | `DivAssign`  |
| `:`                  | `pat: type`, `ident: type`                              | 约束条件                                                      |           |
| `:`                  | `ident: expr`                                           | 结构体字段初始化器                                          |           |
| `:`                  | `'a: loop {...}`                                        | 循环标签                                                      |           |
| `;`                  | `expr;`                                                 | 语句和项目终止符                                            |           |
| `;`                  | `[...; len]`                                            | 固定大小数组语法的一部分                                    |           |
| `<<`                      | `expr << expr`                                          | Left-shift                                                            | `Shl`          |
| `<<=`                     | `var <<= expr`                                          | Left-shift and assignment                                             | `ShlAssign`    |
| `<`                       | `expr < expr`                                           | Less than comparison                                                  | `PartialOrd`   |
| `<=`                      | `expr <= expr`                                          | Less than or equal to comparison                                      | `PartialOrd`   |
| `=`                       | `var = expr`, `ident = type`                            | Assignment/equivalence                                                |                |
| `==`                      | `expr == expr`                                          | Equality comparison                                                   | `PartialEq`    |
| `=>`                   | `pat => expr`                                           | 匹配子句的一部分                                              |          |
| `>`                   | `expr > expr`                                           | 大于比较式                                                | `PartialOrd` |
| `>=`                   | `expr >= expr`                                          | 大于等于比较式                                          | `PartialOrd` |
| `>>`                   | `expr >> expr`                                          | 右移操作                                                      | `Shr`   |
| `>>=`                  | `var >>= expr`                                          | 右移操作及赋值操作                                        | `ShrAssign`  |
| `@`                  | `ident @ pat`                                           | 模式绑定式                                                |          |
| `^`                  | `expr ^ expr`                                           | 按位异或运算                                                | `BitXor`   |
| `^=`                  | `var ^= expr`                                           | 按位异或运算及赋值操作                                   | `BitXorAssign`  |
| <code>&vert;</code>     | <code>pat &vert; pat</code>                             | 模式替代式                                                |          |
| <code>&vert;</code>     | <code>expr &vert; expr</code>                           | 按位或运算                                                      | `BitOr`        |
| <code>&vert;=</code>    | <code>var &vert;= expr</code>                           | 按位或运算及赋值操作                                     | `BitOrAssign`  |
| <code>&vert;&vert;</code> | <code>expr &vert;&vert; expr</code>                     | 逻辑或的短路形式                                        |          |
| `?`                   | `expr?`                                                 | 错误传播                                                     |                |### 非运算符符号

以下表格包含了所有不能作为运算符的符号；
也就是说，它们并不像函数或方法调用那样工作。

表B-2列出了那些可以独立出现且在多种情况下有效的符号。
地点。

<span class="caption"> 表B-2：独立语法</span>


| 符号                                                                 | 说明                                                                 |
| ---------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| `'ident`                                                               | 命名生命周期或循环标签                                           |
| 紧跟在Digit之后的 `u8`, `i32`, `f64`, `usize` 等        | 特定类型的数字字面量                                       |
| `"..."`                                                                | 字符串字面量                                                     |
| `r"..."`, `r#"..."#`, `r##"..."##` 等                          | 原始字符串字面量；未处理的转义字符                          |
| `b"..."`                                                               | 字节字符串字面量；构造字节数组而非字符串                    |
| `br"..."`, `br#"..."#`, `br##"..."##` 等                       | 原始字节字符串字面量；原始字符串与字节字符串的结合           |
| `'...'`                                                                | 字符字面量                                                     |
| `b'...'`                                                               | ASCII字节字面量                                                |
| <code>&vert;...&vert; expr</code>                          | 闭包                                                                |
| `!`                                                                    | 用于发散函数的始终为空的下标类型                          |
| `_`                                                                    | “忽略”的模式绑定；也用于使整数字面量可读                |表B-3列出了在模块路径上下文中出现的符号。
将层次结构转换为单个项目。

<span class="caption"> 表B-3：与路径相关的语法</span>


| 符号                                      | 解释                                                                                                   |
| --------------------------------------- | -------------------------------------------------------------------------------------------------------------|
| `ident::ident`                               | 命名空间路径                                                                                             |
| `::path`                               | 相对于 crate 根的路径（即，明确的绝对路径）       |
| `self::path`                            | 相对于当前模块的路径（即，明确的相对路径）       |
| `super::path`                           | 相对于当前模块父级模块的路径                  |
| `type::ident`, `<type as trait>::ident`                   | 相关的常量、函数和类型                        |
| `<type>::...`                           | 无法直接命名的类型相关的项（例如 `<&T>::...`、 `<[T]>::...` 等） |
| `trait::method(...)`                    | 通过命名定义该方法的 trait 来消除歧义        |
| `type::method(...)`                     | 通过命名该方法所定义的类型来消除歧义         |
| `<type as trait>::method(...)`          | 通过同时命名 trait 和类型来消除歧义           |表B-4列出了在使用泛型类型的上下文中出现的符号。
参数。

<span class="caption"> 表B-4：泛型  
</span>


| 符号                           | 解释                                                                                                 |
| ------------------------------ | ----------------------------------------------------------------------------------- |
| `path<...>`                    | 指定类型中泛型类型的参数（例如， `Vec<u8>`）                                                                 |
| `path::<...>`, `method::<...>` | 指定表达式中的泛型类型、函数或方法的参数；常被称为 _turbofish_（例如， `"42".parse::<i32>()`） |
| `fn ident<...> ...`            | 定义泛型函数                                                                                         |
| `struct ident<...> ...`        | 定义泛型结构                                                                                         |
| `enum ident<...> ...`          | 定义泛型枚举                                                                                         |
| `impl<...> ...`                | 定义泛型实现                                                                                         |
| `for<...> type`                | 更高阶的生命周期限制                                                                                   |
| `type<ident=type>`             | 一种泛型类型，其中一个或多个关联类型具有特定的赋值（例如， `Iterator<Item=T>`） |表B-5列出了在限制泛型类型的上下文中出现的符号。
具有特征边界的参数。

<span class="caption"> 表B-5：特质绑定约束</span>


| 符号                           | 解释                                                                                                                                |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `T: U`                      | 通用参数 `T`，受限于实现了 `U` 的类型                                                                                   |
| `T: 'a`                      | 通用类型 `T`，必须超出生命周期 `'a`（意味着该类型不能包含任何生命周期短于 `'a` 的引用） |
| `T: 'static`                  | 通用类型 `T`，不包含除 `'static` 之外的借用引用                                                                               |
| `'b: 'a`                      | 通用生命周期 `'b`，必须超出生命周期 `'a`                                                                                   |
| `T: ?Sized`                   | 允许通用类型参数为动态大小的类型                                                                                                        |
| `'a + trait`, `trait + trait` | 复合类型约束                                                                                                                   |表B-6列出了在调用或定义过程中出现的符号。
宏以及在一个项目上指定属性。

<span class="caption"> 表B-6：宏与属性</span>


| 符号                                      | 解释                  |
| ------------------------------------------- | ------------------ |
| `#[meta]`                                   | 外部属性              |
| `#![meta]`                                  | 内部属性              |
| `$ident`                                    | 宏替换               |
| `$ident:kind`                               | 宏元变量              |
| `$(...)...`                                 | 宏重复               |
| `ident!(...)`, `ident!{...}`, `ident![...]` | 宏调用               |表B-7列出了用于创建注释的符号。

<span class="caption">表B-7：注释</span>


| 符号         | 解释                         |
| ------------ | --------------------------- |
| `//`     | 行注释                       |
| `//!`     | 内部行文档注释               |
| `///`     | 外部行文档注释               |
| `/*...*/`     | 块注释                       |
| `/*!...*/`     | 内部块文档注释               |
| `/**...*/`     | 外部块文档注释               |表B-8展示了使用括号的各种情境。

<span class="caption"> 表B-8：圆括号</span>


| 符号                         | 解释                                                                                   |
| ------------------------- | ----------------------------------------------------------------------------------- |
| `()`                   | 空元组（也称为单元），包括字面量和类型 |
| `(expr)`                  | 括号表达式                                                                               |
| `(expr,)`                  | 单元素元组表达式                                                                          |
| `(type,)`                  | 单元素元组类型                                                                          |
| `(expr, ...)`                  | 元组表达式                                                                               |
| `(type, ...)`                  | 元组类型                                                                               |
| `expr(expr, ...)`                  | 函数调用表达式；也用于初始化元组 `struct`s 和元组 `enum` 变体 |表B-9展示了使用花括号的各种情境。

<span class="caption"> 表B-9：花括号


| 上下文         | 解释               |
| -------------- | ---------------- |
| `{...}`       | 块表达式            |
| `Type {...}`       | 结构体字面量        |表B-10列出了使用方括号的各种情境。

<span class="caption"> 表B-10：方括号</span>


| 上下文                                              | 解释                                                                                                                   |
|----------------------------------------------------------| ----------------------------------------------------------------------------------------------------------------------------- |
| `[...]`                                          | 数组字面量                                                                                                                 |
| `[expr; len]`                                        | 包含 `len` 个 `expr` 的副本的数组字面量                                                                                   |
| `[type; len]`                                        | 包含 `len` 个 `type` 实例的数组类型                                                                                   |
| `expr[expr]`                                       | 集合索引；可重载（`Index`, `IndexMut`）                                                                                   |
| `expr[..]`, `expr[a..]`, `expr[..b]`, `expr[a..b]` | 假装为集合切片的集合索引，使用 `Range`, `RangeFrom`, `RangeTo`, 或 `RangeFull` 作为“索引” |