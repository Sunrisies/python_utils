# 枚举与模式匹配

在本章中，我们将探讨枚举类型，也称为_enums`。  
枚举允许我们通过列举其可能的取值来定义一种类型。首先，我们将定义一个枚举，并展示如何利用枚举来同时编码数据和含义。接下来，我们将介绍一个特别有用的枚举类型，称为`Option`，它表示一个值可以是某种状态或者不存在。然后，我们将探讨`match`表达式中的模式匹配功能，以及如何根据枚举的不同取值来执行不同的代码。最后，我们将介绍`if let`构造符，这是一种方便且简洁的语法，可用于在代码中处理枚举类型。