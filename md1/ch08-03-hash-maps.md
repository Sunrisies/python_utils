## 在哈希映射中存储带有关联值的键

我们常见的数据结构之一就是哈希表。类型 `HashMap<K, V>` 存储了键的类型 `K` 与值的类型 `V` 之间的映射关系，这种映射是通过一个 _哈希函数_ 来确定的，该函数决定了这些键和值在内存中的存储位置。许多编程语言都支持这种数据结构，但它们通常使用不同的名称来表示它，比如 _哈希表_、_映射表_、_对象_、_字典_或者_关联数组_等等。

哈希映射在需要以非索引方式查找数据时非常有用，就像使用向量一样，但这里的键可以是任何类型的数据。例如，在一个游戏中，你可以将每个队伍的得分存储在哈希映射中，其中每个键是队伍的名称，值则是每个队伍的得分。给定一个队伍名称，你就可以获取该队伍的得分。

在本节中，我们将介绍哈希映射的基本API，但标准库在`HashMap<K, V>`上定义的函数中还包含许多其他有用的功能。如常，如需更多信息，请查阅标准库的文档。

### 创建新的哈希映射

创建空哈希映射的一种方法是使用`new`来添加元素，并使用`insert`来添加具体的值。在清单8-20中，我们记录了两支队伍的分数，这两支队伍的名字分别是_Blue_和_Yellow_。蓝队以10分开始，黄队则以50分开始。

<listing number="8-20" caption="创建新的哈希映射并插入一些键和值">

```rust
{{#rustdoc_include ../listings/ch08-common-collections/listing-08-20/src/main.rs:here}}
```

</ Listing>

请注意，我们需要首先从标准库的集合部分中 `use` 导入 `HashMap`。在我们的三个常用集合中，这个集合使用频率最低，因此不会自动包含在序言中介绍的功能中。哈希映射在标准库中的支持也相对较少；例如，标准库中没有用于构建哈希映射的内置宏。

与向量一样，哈希映射也将数据存储在堆上。这个`HashMap`的键类型为`String`，值类型为`i32`。与向量类似，哈希映射也是同质的：所有的键必须属于同一种类型，所有的值也必须属于同一种类型。

### 在哈希映射中访问值

我们可以通过向`get`方法提供哈希图的键来获取其值，如清单8-21所示。

<listing number="8-21" caption="访问存储在哈希表中的蓝色队伍的分数">

```rust
{{#rustdoc_include ../listings/ch08-common-collections/listing-08-21/src/main.rs:here}}
```

</ Listing>

在这里，``score``将包含与蓝队相关的数值，而结果则会是``10``。``get``方法会返回一个``Option<&V>``；如果哈希映射中不存在该键对应的数值，那么``get``将会返回``None``。该程序通过调用``copied``来获取``Option<i32>``，而不是``Option<&i32>``；然后，如果``scores``中没有该键的条目，就会通过``unwrap_or``将``score``设置为零。

我们可以以与处理向量类似的方式，使用`for`循环来遍历哈希映射中的每个键值对。

```rust
{{#rustdoc_include ../listings/ch08-common-collections/no-listing-03-iterate-over-hashmap/src/main.rs:here}}
```

这段代码会以任意顺序打印每一对元素：

```text
Yellow: 50
Blue: 10
```

<!-- 旧的标题。不要删除，否则链接可能会失效。-->

<a id="哈希表与所有权"></a>

### 在哈希映射中管理所有权

对于那些实现了 ``Copy`` 特性的类型，比如 ``i32``，其值会被复制到哈希映射中。而对于像 ``String`` 这样的拥有型值，这些值会被移动，而哈希映射将成为这些值的拥有者，如清单 8-22 所示。

<列表编号="8-22" 标题="证明在哈希映射中插入键和值后，它们确实属于该映射">

```rust
{{#rustdoc_include ../listings/ch08-common-collections/listing-08-22/src/main.rs:here}}
```

</ Listing>

在通过调用 `insert` 将变量 `field_name` 和 `field_value` 移入哈希映射之后，我们无法再使用它们了。

如果我们向哈希映射中插入对值的引用，那么这些值并不会被移动到哈希映射中。这些引用所指向的值必须至少与哈希映射的有效期一样长才有效。我们将在第十章的“利用生命周期验证引用”部分进一步讨论这些问题。

### 更新哈希映射

虽然键和值对的数量是可以增长的，但每个唯一的键一次只能关联一个值（反之则不行：例如，Blue团队和Yellow团队都可以将值`10`存储在`scores`哈希表中）。

当你想要修改哈希映射中的数据时，你必须决定如何处理这样一种情况：某个键已经分配了值。你可以将旧值替换为新值，完全忽略旧值；或者保留旧值，只在新键尚未被分配值时才添加新值。又或者可以将旧值和新值合并起来。让我们来看看如何分别实现这些操作！

#### 覆盖一个值

如果我们向哈希映射中插入一个键和一个值，然后再用不同的值插入同一个键，那么该键对应的值将会被替换。尽管清单8-23中的代码两次调用了`insert`，但由于我们两次都插入了蓝色队伍对应键的值，因此哈希映射中只会包含一个键值对。

<列表编号="8-23" 标题="替换存储在特定键的值">

```rust
{{#rustdoc_include ../listings/ch08-common-collections/listing-08-23/src/main.rs:here}}
```

</ Listing>

这段代码将输出`{"Blue": 25}`。`10`的原始值已经被覆盖了。

<!-- 旧的标题。不要删除，否则链接可能会失效。-->

<a id="仅当键没有值时才插入值"></a>

#### 仅在不存在某个键时才添加该键及其对应的值

通常，我们会先检查哈希映射中是否存在某个特定的键以及该键对应的数值，然后根据情况采取以下操作：如果键确实存在于哈希映射中，那么原有的数值保持不变；如果键不存在，则将其插入哈希映射，并为它分配一个数值。

哈希映射有一个专门的API，名为`entry`，它接受一个需要检查的键作为参数。`entry`方法的返回值是一个枚举类型，名为`Entry`，该枚举类型表示可能存在也可能不存在的值。假设我们想要检查黄色队伍对应的键是否有一个关联的值。如果没有，我们就需要将`50`的值插入到哈希映射中；蓝色队伍的情况也是如此。通过使用`entry` API，代码如下所示，如清单8-24所示。

<列表编号="8-24" 标题="使用 `entry` 方法仅在键尚未有值时进行插入">

```rust
{{#rustdoc_include ../listings/ch08-common-collections/listing-08-24/src/main.rs:here}}
```

</ Listing>

在 ``Entry`` 中定义的 ``or_insert`` 方法，其目的是返回一个可变引用，指向对应 ``Entry`` 键的值。如果该键存在，则返回该值；如果不存在，则将该参数作为该键的新值插入，并返回对该新值的可变引用。这种实现方式比我们自己编写逻辑要简洁得多，而且还能更好地与借用检查器协同工作。

运行清单8-24中的代码将会打印出`{"Yellow": 50, "Blue": 10}`。第一次调用`entry`会插入Yellow团队的键值对，其值为`50`，因为Yellow团队还没有这个键值对。第二次调用`entry`则不会改变哈希映射，因为Blue团队已经拥有值`10`了。

#### 根据旧值更新数值

哈希映射的另一个常见用途是查找某个键对应的值，然后根据旧值对其进行更新。例如，清单8-25展示了一段代码，该代码用于统计某些文本中每个单词出现的次数。我们使用一个哈希映射，将单词作为键，并增加其值来记录该单词出现的次数。如果这是我们第一次遇到某个单词，我们会首先将该值设置为`0`。

<listing number="8-25" caption="使用哈希映射来统计单词的出现次数">

```rust
{{#rustdoc_include ../listings/ch08-common-collections/listing-08-25/src/main.rs:here}}
```

</ Listing>

这段代码会打印出`{"world": 2, "hello": 1, "wonderful": 1}`。您可能会看到相同的键值对以不同的顺序被打印出来：回想一下在[“在哈希映射中访问值”][access]中的内容，会发现遍历哈希映射的过程是随机的。

`split_whitespace`方法返回一个迭代器，该迭代器遍历由空白字符分隔的子切片，这些子切片对应于`text`中的值。`or_insert`方法返回一个可变引用（指向`&mut V`中的值），该引用可用于获取指定键对应的值。在这里，我们将这个可变引用存储在`count`变量中，因此要对该值进行赋值，必须先使用星号运算符（`*`）解除对`count`的引用。这个可变引用在`for`循环结束时就会超出作用域，因此所有这些修改都是安全的，并且符合借用规则。

### 哈希函数

默认情况下，`HashMap`使用一种名为_SipHash_的哈希函数，该函数能够抵抗涉及哈希表的拒绝服务攻击[^siphash]<!-- ignore -->。这并不是最快的哈希算法，但为了提高安全性而不得不接受的性能下降是值得的。如果你对代码进行性能分析，发现默认的哈希函数对于你的需求来说过于缓慢，那么可以通过指定不同的哈希器来更换该函数。_哈希器_是一种实现了`BuildHasher`特性的类型。我们将在[第10章][traits]<!-- ignore -->中讨论特性以及如何实现它们。你不必从零开始自己实现哈希器；[crates.io](https://crates.io/)<!-- ignore -->上有许多其他Rust用户共享的库，这些库提供了实现多种常见哈希算法的哈希器。

[^siphash]: [https://en.wikipedia.org/wiki/SipHash](https://en.wikipedia.org/wiki/SipHash)

## 总结

向量、字符串和哈希映射提供了在程序中存储、访问和修改数据所需的大量功能。以下是一些你现在应该能够解决的练习：

1. 给定一个整数列表，使用向量来返回该列表的中位数（排序后的中间位置的值）和众数（出现次数最多的数值；这里使用哈希映射会非常有帮助）。

1. 将字符串转换为Pig Latin。每个单词的第一个辅音会被移动到单词的末尾，并加上“ay”，例如“first”变成“irst-fay”。以元音开头的单词则会在末尾加上“hay”（例如“apple”变成“apple-hay”）。请注意UTF-8编码的相关细节！

1. 使用哈希映射和向量，创建一个文本界面，允许用户向公司中的某个部门添加员工姓名。例如，“将Sally添加到工程部”或“将Amir添加到销售部”。然后，用户可以检索某个部门的所有人员名单，或者按部门排序的公司所有人员的名单。

标准库API文档中描述了向量、字符串以及哈希映射所具有的方法，这些方法对完成这些练习非常有帮助！

我们正在处理更复杂的程序，在这些程序中，操作可能会失败，因此现在是讨论错误处理的最佳时机。接下来我们将进行这方面的讨论！

[验证引用与生命周期]: ch10-03-lifetime-syntax.html#validating-references-with-lifetimes  
[访问]: #在哈希映射中访问值  
[特性]: ch10-02-traits.html